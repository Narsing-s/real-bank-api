# Local development only. Never store real credentials in Git.
http:
  listener:
    host: "localhost"
    port: "8081"
  requester:
    host: "api.ultramsg.com"
    method: "POST"
    path: "/instance184029/messages/chat"
db:
  sf:
    name: "YOUR_SNOWFLAKE_ACCOUNT"
    warehouse: "COMPUTE_WH"
    database: "MYDATABASE"
    schema: "PUBLIC"
    user: "YOUR_SNOWFLAKE_USER"
    password: "SET_LOCALLY"
    role: "ACCOUNTADMIN"
email:
  host: "smtp.gmail.com"
  port: "587"
  username: "YOUR_EMAIL"
  password: "SET_LOCALLY"
